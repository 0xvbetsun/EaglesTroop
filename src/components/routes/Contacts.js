import React from 'react';
import PropTypes from 'prop-types';

const Contacts = props => {
  return (
    <div>
      <h1>Contacts</h1>
    </div>
  );
};

Contacts.propTypes = {};

export default Contacts;
